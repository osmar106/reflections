What happens when you initialize a repository? Why do you need to do it?
Terminal creates a .git file, to do it its necessary use the command git init in the folder where we want to create it

How is the staging area different from the working directory and the repository? What value do you think it offers?
The staging area is space between the working directory and the repository. There we can add all the files required previous make a commit

How can you use the staging area to make sure you have one commit per logical change?
I can use it by checking what is it in everytime before make a commit

What are some situations when branches would be helpful in keeping your history organized? How would branches help?
When I want add a new feature or a new languaje, branches can also help to keep your master safe.

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?
The result is a code with the improves of both branches. Because is the easiest way to understand the changes in the code.

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?
Pros: automatic merging is easier and faster.
Cons: you can't modifity your code a lot if you want to make an automatic merge.

